name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (Ruff)
        run: ruff check .

      - name: Type check (MyPy)
        run: mypy src

      - name: Run tests
        run: pytest --cov=ssh_logstream --cov-report=term-missing

  build:
    name: Build distributions
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build distributions
        run: python -m build

      - name: Check distributions
        run: twine check dist/*

      - name: Smoke test wheel
        run: |
          python -m venv .venv-wheel
          . .venv-wheel/bin/activate
          python -m pip install --upgrade pip
          pip install --no-cache-dir dist/*.whl
          python - <<'PY'
          import importlib.resources as resources
          import ssh_logstream
          from ssh_logstream import LineStreamer, LineStreamerConfig, SshConfig, StreamStats

          print("imports ok")
          assert LineStreamer is not None
          assert LineStreamerConfig is not None
          assert SshConfig is not None
          assert StreamStats is not None

          py_typed = resources.files("ssh_logstream").joinpath("py.typed")
          assert py_typed.is_file(), "py.typed missing"
          print("py.typed present")
          PY

      - name: Smoke test sdist
        run: |
          python -m venv .venv-sdist
          . .venv-sdist/bin/activate
          python -m pip install --upgrade pip
          pip install --no-cache-dir dist/*.tar.gz
          python - <<'PY'
          import importlib.resources as resources
          import ssh_logstream
          from ssh_logstream import LineStreamer, LineStreamerConfig, SshConfig, StreamStats

          print("imports ok")
          assert LineStreamer is not None
          assert LineStreamerConfig is not None
          assert SshConfig is not None
          assert StreamStats is not None

          py_typed = resources.files("ssh_logstream").joinpath("py.typed")
          assert py_typed.is_file(), "py.typed missing"
          print("py.typed present")
          PY

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/ssh-logstream
    permissions:
      id-token: write

    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
